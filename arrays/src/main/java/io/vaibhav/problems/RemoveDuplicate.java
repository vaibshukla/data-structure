package io.vaibhav.problems;

public class RemoveDuplicate {


    /***
     *  Remove Duplicates from array , Given sorted array return the size of distinct element
     *  I/P : [10, 20 ,20 ,30 ,30,40]
     *  O/P : 3
     */
    public int  removeDuplicate(int[] arr ){
        int size = arr.length;
        int newSize = 1;
        for (int i = 0; i < size ; i++) {
            if (arr[newSize-1] != arr[0]) {

            }
        }

        return 0;
    }
}
